// This function runs only at the start of the game.
fun Start()
{
	Debug.Log("Hello, World!");
}

// This function runs every frame.
fun Update()
{
	Debug.Log("Tick");

	// This should move the camera 0.01 closer every frame
	Camera.position.z += 0.01;
}